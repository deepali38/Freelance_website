
<div class="container-fluid">
<div class="container">
  <div class="row">
  
    <div class="col-md-2 mt-6">
      <span class="Text fs-3 mb-4 fw-bold"><%= @current_user.name_or_company_name%></span>
      <h4 class="mb-2 mt-4 fw-bolder "> Contacts </h4>
      <div>
        <%= turbo_stream_from "users" %>
        <div id="users">
            
            <%= render @users %>
        </div>
        
      </div>

    <!--<h4> Rooms </h4>
    <%= render partial: "layouts/new_room_form" %>
    <div>
      <%= turbo_stream_from "rooms" %>
      <div id="rooms">
        <%= render @rooms %>
      </div>
    </div>-->
    </div>
  

  <div class="col-md-10 mt-2 text-light shadow p-3 mb-5 bg-white rounded">
    <% if @single_room %>
    <h4 class="text-center text-dark"> <%= @user&.name_or_company_name ||@single_room.name %> </h4>
    <%= turbo_stream_from @single_room %>
    <div id="messages">
       <%= render @messages %>
    </div>
    <%= render partial: 'layouts/new_message_form'%>
  <% end %>
  </div>
  </div>
</div>
</div>


